{
  "name": "one-more-column",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "test": "node --test engines/*.test.js engine/*.test.js lib/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "vercel": "^44.2.7"
  }
}
